import gsap, {Circ, Power3} from 'gsap';
import SplitText from 'gsap/dist/SplitText';


const WorkItemHover = (isHover, title, image) => {

  const tl = gsap.timeline();

  if (isHover){

  }
  else{

  }
}

export default WorkItemHover;